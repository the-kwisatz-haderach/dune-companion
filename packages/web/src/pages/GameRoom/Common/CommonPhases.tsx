import { ReactElement } from 'react'
import { Box, Typography } from '@material-ui/core'

export default function CommonPhases(): ReactElement {
  return (
    <Box p={1}>
      <Typography>Common Phase Screen</Typography>
    </Box>
  )
}
