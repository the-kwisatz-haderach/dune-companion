{
  "name": "dune",
  "version": "0.1.22",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "module": "lib-esm/index.js",
  "jsnext:main": "lib-esm/index.js",
  "files": [
    "dist",
    "lib",
    "lib-esm"
  ],
  "author": "Gustaf Lundström",
  "license": "MIT",
  "sideEffects": false,
  "scripts": {
    "build:lib:umd": "tsc",
    "build:lib:es": "tsc -m es6 --outDir lib-esm",
    "build:dist": "webpack",
    "build": "yarn run clean && yarn run build:lib:umd && yarn run build:lib:es && yarn run build:dist",
    "pack": "yarn build && yarn version --patch && yarn pack",
    "dev": "tsc --watch",
    "ts:defs": "tsc --declaration --outDir './build' --emitDeclarationOnly",
    "test": "jest --watchAll",
    "clean": "rm -rf lib lib-esm dist || rm *.tgz"
  },
  "dependencies": {
    "@reduxjs/toolkit": "^1.5.0"
  },
  "devDependencies": {
    "@babel/core": "^7.8.4",
    "awesome-typescript-loader": "^5.2.1",
    "babel-loader": "^8.0.6",
    "clean-webpack-plugin": "^3.0.0",
    "uglifyjs-webpack-plugin": "^2.2.0",
    "webpack": "^4.41.5",
    "webpack-cli": "^3.3.10",
    "webpack-node-externals": "^2.5.2"
  }
}
